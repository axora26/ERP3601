# Windows Desktop

**Status: NOT STARTED.** No desktop packaging exists in this repository.
The mission brief's target layout names `apps/desktop`, but it was
deliberately not scaffolded in Wave 1 (see `docs/PRODUCT_ARCHITECTURE.md`)
— an empty shell with a launcher and no application to launch would be
exactly the "template scaffolding no one uses" the project avoids.

This is scheduled as Wave 13 in `docs/IMPLEMENTATION_ROADMAP.md`, once
there is enough of the product built that a native wrapper is worth
qualifying with a real installation smoke test (per mission Section 56 —
"do not declare the Windows version functional before a real install
smoke").
